# 环境变量样例：复制成 .env 再填值（.env 已被 .gitignore 忽略，不要提交）
#
# 边界：这里只放本地/测试环境的值。生产密钥不进任何 git，由部署平台的密钥
# 管理服务在启动时注入（Vault / K8s Secret / 云厂商 Secret Manager）。
#
# 生成一把随机签名密钥：
#   openssl rand -base64 48

# JWT 签名密钥（HS256 至少 32 字节）。缺失时应用启动即失败，不会退回仓库里的 key
JWT_SECRET=replace-with-a-random-string-at-least-32-bytes
