# 可选：换成真密钥部署时才需要，本地开箱即用不需要这个文件。
#
# 边界说明（与 README「安全防护」一节一致）：
#   dev / docker profile 各带一把仅本地可用的默认 key，克隆下来直接 compose up 就能跑；
#   prod profile 是 ${JWT_SECRET}（无默认值），缺失时 JwtUtil 直接拒绝启动。
#   真正部署时把本文件复制成 .env（.env 已被 .gitignore 忽略）并由编排注入，
#   或者改用平台密钥服务（Vault / 云厂商 Secret Manager / K8s Secret）。
#
# 生成一把随机签名密钥：openssl rand -base64 48

JWT_SECRET=replace-with-a-random-string-at-least-32-bytes
